<!DOCTYPE html>
<html>
<head>
    <title>Register - <%= shopData.shopName %></title>

    <style>
        body {
            font-family: "Poppins", sans-serif;
            background: linear-gradient(135deg, #6dd5fa, #2980b9);
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            background: #fff;
            width: 430px;
            padding: 35px;
            border-radius: 16px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            animation: fadeIn 0.6s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        h1 {
            text-align: center;
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 28px;
        }

        .error-box {
            background: #ffebee;
            border-left: 5px solid #e74c3c;
            padding: 10px 15px;
            margin-bottom: 20px;
            border-radius: 6px;
            color: #c0392b;
        }

        .input-group {
            margin-bottom: 18px;
            text-align: left;
        }

        label {
            font-size: 15px;
            font-weight: 600;
            display: block;
            margin-bottom: 6px;
            color: #34495e;
        }

        input {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 15px;
            transition: 0.2s;
        }

        input:focus {
            border-color: #3498db;
            box-shadow: 0 0 5px rgba(52,152,219,0.5);
            outline: none;
        }

        .submit-btn {
            margin-top: 10px;
            width: 100%;
            padding: 14px;
            background: #3498db;
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: 0.25s;
        }

        .submit-btn:hover {
            background: #2980b9;
            transform: translateY(-2px);
        }

        .back-home {
            display: block;
            margin-top: 20px;
            text-align: center;
            font-size: 15px;
            color: #2c3e50;
            text-decoration: none;
        }

        .back-home:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

<div class="card">
    <h1>Register for <%= shopData.shopName %></h1>

    <!-- 🔥 HATA MESAJLARI GÖSTERME -->
    <% if (errors && errors.length > 0) { %>
        <div class="error-box">
            <ul>
                <% errors.forEach(err => { %>
                    <li><%= err.msg %></li>
                <% }) %>
            </ul>
        </div>
    <% } %>

    <form method="POST" action="registered">

        <div class="input-group">
            <label for="first">First Name</label>
            <input id="first" type="text" name="first" value="<%= typeof first !== 'undefined' ? first : '' %>" required>
        </div>

        <div class="input-group">
            <label for="last">Last Name</label>
            <input id="last" type="text" name="last" value="<%= typeof last !== 'undefined' ? last : '' %>" required>
        </div>

        <div class="input-group">
            <label for="email">Email</label>
            <input id="email" type="email" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" required>
        </div>

        <div class="input-group">
            <label for="username">Username</label>
            <input id="username" type="text" name="username" value="<%= typeof username !== 'undefined' ? username : '' %>" required>
        </div>

        <div class="input-group">
            <label for="password">Password</label>
            <input id="password" type="password" name="password" required>
        </div>

        <button class="submit-btn" type="submit">Create Account</button>
    </form>

    <a href="/" class="back-home">← Return to Home</a>
</div>

</body>
</html>